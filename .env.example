# Copy this structure to `.env` for local use. The app loads `.env`
# automatically with python-dotenv. Never commit a file containing real keys.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
FINALFLOW_MAX_AI_REQUESTS_PER_SESSION=10
SERPAPI_API_KEY=
