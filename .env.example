# Copy this file to `.env` for local development.
# Never commit `.env` or files containing real API keys.
# These keys are intended for backend/server-side use only.
# AWS variables will be documented later when deployment work begins.

OPENAI_API_KEY=
SERPAPI_API_KEY=
